# 👑 Monopoly Legacy — Server

Authoritative server untuk game Monopoly Legacy.

## Cara Menjalankan

\`\`\`bash
npm install
npm run seed
npm start
\`\`\`

Server akan hidup di `http://localhost:4000`.

Akun default: `MUTTAQI2215` / `ChangeMe_2025!`
Admin password default: `changeme-admin`
